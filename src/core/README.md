# Core Components

This directory contains the core components of the ZCAM MCP server.

## Components

### CameraManager.ts
Manages the collection of Z CAM cameras, including discovery, addition, and listing of cameras. It also handles camera context switching and status management.

### ConfigManager.ts
Handles configuration management including favorite cameras, camera history, and persistent storage of settings.